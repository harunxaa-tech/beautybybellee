AngebotsPilot v11.31.15 – ROOT Upload

Bitte diese Dateien in den ROOT des GitHub-Repositories hochladen/ersetzen:
- cloud-config.js
- service-worker.js
- compliance-v113115.js (NEU)

Nichts löschen und keine Geschäftsdaten verändern.

Supabase ist bereits vorbereitet:
- Migration v11_31_15_invoice_validation_events ist aktiv.
- Edge Function xrechnung-validate v2 ist aktiv und verlangt JWT.

Wichtig:
Die neue Serverprüfung speichert keine Rechnungs-XML in der Audit-Tabelle, sondern nur Prüfsumme und Ergebnis-Metadaten. Der offizielle KoSIT-Erfolg wird erst dann als bestanden angezeigt, wenn ein eigener KoSIT-Daemon serverseitig verbunden ist. Bis dahin bleibt der Status ausdrücklich „KoSIT noch ausstehend“.
