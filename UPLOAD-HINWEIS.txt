AngebotsPilot v11.31.18

ROOT-Upload: Diese Dateien in das Repository-Root hochladen/ersetzen:
- cloud-config.js
- service-worker.js
- compliance-v113118.js (neu)

Nichts löschen. Geschäftsdaten bleiben unverändert.

Fix: Offizieller KoSIT-Test hat erstmals die echte XML-Schemaabweichung gefunden.
LegalMonetaryTotal wird nun in UBL-Schema-Reihenfolge erzeugt:
LineExtensionAmount → TaxExclusiveAmount → TaxInclusiveAmount → AllowanceTotalAmount → ... → PayableAmount.

Die Rechnung bleibt bei jeder fehlgeschlagenen KoSIT-Prüfung Entwurf.
