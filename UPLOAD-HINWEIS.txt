AngebotsPilot v11.31.27 – Stabilitäts-/Aufräumrelease

Im Repository-ROOT ersetzen/hochladen:
- cloud-config.js
- cloud-sync.js
- service-worker.js
- compliance-v113126.js
- manifest.json

Bereinigt/gehärtet:
- manueller Cloud-Sync wartet jetzt sicher auf laufende/queued Synchronisierungen
  (kein Pull mehr, bevor der lokale Push wirklich abgeschlossen ist)
- Compliance-Loader leitet Dateinamen zentral aus der aktuellen Quelle ab; keine
  veraltete Versions-Regex mehr, die neue KoSIT-Fixes blockieren kann
- Service-Worker-Cache auf einen konsistenten v11.31.27-Stand gebracht und
  Offline-Fallback query-unabhängig gemacht
- Runtime-Diagnose AP Runtime Diagnostics ergänzt
- validierter KoSIT/XRechnung-Pfad aus v11.31.25 unverändert erhalten

Backend wurde separat ohne Geschäftsdatenänderung bereinigt:
- alle fehlenden Fremdschlüssel-Indizes ergänzt
- doppelten Rechnungs-Index entfernt
- fehlerhafte Mandantenprüfung bei Baustellen-Zuweisungen korrigiert
- RLS-Policies auf performante auth()-Auswertung umgestellt
- doppelte permissive Team-Einladungs-UPDATE-Policy zusammengeführt
- Datenintegrität erneut geprüft: keine doppelten Rechnungsnummern, keine
  ausgestellten Rechnungen ohne Positionen, keine verwaisten Nummernreservierungen
  und keine mandantenfremden Baustellen-Zuweisungen

Zusätzlich: veraltete Versions-/Stripe-Testbeschreibung aus dem PWA-Manifest entfernt.

Zusätzlich im eigentlichen Sync-Kern behoben:
- der alte manual()-Pfad setzt syncing nicht mehr vor pushSnapshot(); der Fix steckt jetzt
  direkt in cloud-sync.js und nicht nur in einer Runtime-Umleitung
- CloudSync meldet die aktuelle Kernversion 11.31.27 für Diagnose/QA


v11.31.27 zusätzlich:
- XRechnung-Storno/Gutschrift erhält BG-16 Zahlungsanweisung (KoSIT BR-DE-1).
- KoSIT-Validierung läuft vor der unveränderbaren Archivierung; abgelehnte XML wird nicht archiviert.
- Lokaler Vorabcheck erkennt fehlende Zahlungsanweisung/IBAN vor dem Serverlauf.
